node_modules/
verification-output/

# Foto asli sebelum dikompres (scripts/compress-images.py) — cadangan lokal saja
assets/img_backup/
.env
*.log

# graphify-axet (tool lokal — tidak ikut repo)
.graphify/
.graphifyignore

# Tooling/session lokal Hermes
.hermes/

# Hasil dump schema Supabase untuk inspeksi lokal (bukan migration)
supabase/remote-public-schema.sql
supabase/*-schema.sql

# Artifact build GitHub Pages (dibuat ulang oleh scripts/build-pages-dist.mjs)
dist/
