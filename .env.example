# Salin file ini menjadi .env lalu isi nilainya (jangan commit .env — sudah di .gitignore).
# Service role key: Supabase Dashboard → Settings → API → Project API keys → service_role.
# Kunci ini melewati seluruh RLS — HANYA untuk skrip lokal (seed), tidak pernah untuk admin.html.
SUPABASE_URL=https://rxqolwczphehbzrzmisa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
