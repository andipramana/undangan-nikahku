import fs from 'node:fs/promises';
const [html, js, fn, css] = await Promise.all(['register.html','assets/js/register.js','supabase/functions/provision-invitation/index.ts','assets/css/admin.css'].map(p=>fs.readFile(p,'utf8')));
for (const [src, token] of [[html,'id="clients-panel"'],[html,'id="clients-list"'],[js,'action: "list"'],[js,'data-client-edit'],[js,'data-client-toggle'],[js,'data-client-delete'],[js,'Ketik HAPUS'],[fn,'action === "list"'],[fn,'action === "update"'],[fn,'action === "delete"'],[fn,'admin.auth.admin.deleteUser'],[fn,'admin.schema("storage").from("objects")'],[css,'.client-row']]) if (!src.includes(token)) throw new Error(`Missing client-management contract: ${token}`);
console.log('PASS: root client list supports edit, activate/deactivate, and guarded permanent deletion including tenant storage and isolated Auth users.');
